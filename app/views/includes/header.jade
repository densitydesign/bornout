header#header
  .navbar.navbar-fixed-top(role='navigation')
    .navbar-inner
      .container
        a.brand(href='/') WHO
        .nav-collapse.collapse
          ul.nav
            li(class=isActive('/sections/new') ? 'active' : '')
              a(href="/visualizations/new", title="new article") New
          ul.nav.pull-right

            if (req.isAuthenticated())
              li
                a(href="/users/"+req.user.id, title="Profile") Profile
              li
                a(href="/logout", title="logout") Logout
            else
              li
                a(href="/login", title="Login") Login
